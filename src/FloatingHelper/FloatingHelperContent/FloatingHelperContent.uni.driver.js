// import { baseUniDriverFactory } from 'wix-ui-test-utils/base-driver';
// import { dataHooks } from './constants';
// import { SKINS } from '../../Button/constants';
//
// export const floatingHelperContentDriverFactory = (base, body) => {
//   return {
//     ...baseUniDriverFactory(base, body),
//
//     /** Get the current count */
//     getCountText: async () =>
//       base.$(`[data-hook="${dataHooks.floatingHelperContentCount}"]`).text(),
//
//     /** Click the button */
//     clickButton: async () =>
//       base.$(`[data-hook="${dataHooks.floatingHelperContentButton}"]`).click(),
//
//     /** Get the button's text */
//     getButtonText: async () =>
//       base.$(`[data-hook="${dataHooks.floatingHelperContentButton}"]`).text(),
//   };
// };
